From 68f924be72536a105d4291ca86393b4bb8882d5d Mon Sep 17 00:00:00 2001
From: Agatha-luna <magambo@beldex.io>
Date: Tue, 7 Jul 2026 23:38:48 +0530
Subject: [PATCH 08/16] Add recoverDeterministicWalletFromSpendKey

---
 src/wallet/api/wallet.cpp         | 27 +++++++++++++++++++++++++++
 src/wallet/api/wallet.h           |  4 ++++
 src/wallet/api/wallet2_api.h      | 19 +++++++++++++++++++
 src/wallet/api/wallet_manager.cpp | 17 +++++++++++++++++
 src/wallet/api/wallet_manager.h   |  7 +++++++
 5 files changed, 74 insertions(+)

diff --git a/src/wallet/api/wallet.cpp b/src/wallet/api/wallet.cpp
index 7bc4dde39..97a007d0c 100755
--- a/src/wallet/api/wallet.cpp
+++ b/src/wallet/api/wallet.cpp
@@ -850,6 +850,33 @@ bool WalletImpl::recover(std::string_view path_, const std::string &password, co
     return good();
 }
 
+EXPORT
+bool WalletImpl::recoverDeterministicWalletFromSpendKey(std::string_view path_, const std::string &password, const std::string &language, const std::string &spendkey_string)
+{
+    clearStatus();
+
+    m_recoveringFromSeed = true;
+    m_recoveringFromDevice = false;
+
+    auto path = fs::u8path(path_);
+    // parse spend key
+    crypto::secret_key spendkey;
+    bool has_spendkey = !spendkey_string.empty();
+    if (has_spendkey && !tools::hex_to_type(spendkey_string, unwrap(unwrap(spendkey))))
+    {
+        setStatusError(tr("failed to parse secret spend key"));
+        return false;
+    }
+
+    try {
+        wallet()->generate(path, password, spendkey, true, false);
+        setSeedLanguage(language);
+    } catch (const std::exception &e) {
+        setStatusCritical(e.what());
+    }
+    return good();
+}
+
 EXPORT
 bool WalletImpl::close(bool store)
 {
diff --git a/src/wallet/api/wallet.h b/src/wallet/api/wallet.h
index 7bd4e02eb..b847ffc30 100755
--- a/src/wallet/api/wallet.h
+++ b/src/wallet/api/wallet.h
@@ -83,6 +83,10 @@ public:
     bool open(std::string_view path, const std::string &password);
     bool recover(std::string_view path,const std::string &password,
                             const std::string &seed, const std::string &seed_offset = {});
+    bool recoverDeterministicWalletFromSpendKey(std::string_view path_,
+                                                    const std::string &password,
+                                                    const std::string &language,
+                                                    const std::string &spendkey_string);
     bool recoverFromKeysWithPassword(std::string_view path,
                             const std::string &password,
                             const std::string &language,
diff --git a/src/wallet/api/wallet2_api.h b/src/wallet/api/wallet2_api.h
index 8882cce75..c8fb2d1b3 100755
--- a/src/wallet/api/wallet2_api.h
+++ b/src/wallet/api/wallet2_api.h
@@ -1402,6 +1402,25 @@ struct WalletManagerBase
             const std::string& spendKeyString = "",
             uint64_t kdf_rounds = 1) = 0;
 
+    /*!
+     * \brief  recover deterministic wallet from spend key.
+     * \param  path           Name of wallet file to be created
+     * \param  password       Password of wallet file
+     * \param  language       language
+     * \param  nettype        Network type
+     * \param  restoreHeight  restore from start height
+     * \param  spendKeyString spend key
+     * \param  kdf_rounds     Number of rounds for key derivation function
+     * \return                Wallet instance (Wallet::status() needs to be called to check if recovered successfully)
+     */
+    virtual Wallet* createDeterministicWalletFromSpendKey(std::string_view path,
+                                                           const std::string &password,
+                                                           const std::string &language,
+                                                           NetworkType nettype,
+                                                           uint64_t restoreHeight,
+                                                           const std::string &spendkey_string,
+                                                           uint64_t kdf_rounds = 1) = 0;
+
     /*!
      * \brief  creates wallet using hardware device.
      * \param  path                 Name of wallet file to be created (utf-8)
diff --git a/src/wallet/api/wallet_manager.cpp b/src/wallet/api/wallet_manager.cpp
index 4c69e1af9..58190095d 100755
--- a/src/wallet/api/wallet_manager.cpp
+++ b/src/wallet/api/wallet_manager.cpp
@@ -104,6 +104,23 @@ Wallet* WalletManagerImpl::createWalletFromKeys(std::string_view path,
     return wallet;
 }
 
+EXPORT
+Wallet* WalletManagerImpl::createDeterministicWalletFromSpendKey(std::string_view path,
+                                                                 const std::string &password,
+                                                                 const std::string &language,
+                                                                 NetworkType nettype,
+                                                                 uint64_t restoreHeight,
+                                                                 const std::string &spendkey_string,
+                                                                 uint64_t kdf_rounds)
+{
+    WalletImpl* wallet = new WalletImpl(nettype, kdf_rounds);
+    if(restoreHeight > 0){
+        wallet->setRefreshFromBlockHeight(restoreHeight);
+    }
+    wallet->recoverDeterministicWalletFromSpendKey(path, password, language, spendkey_string);
+    return wallet;
+}
+
 EXPORT
 Wallet* WalletManagerImpl::createWalletFromDevice(std::string_view path,
                                                   const std::string &password,
diff --git a/src/wallet/api/wallet_manager.h b/src/wallet/api/wallet_manager.h
index 16b9adeeb..63feea07f 100755
--- a/src/wallet/api/wallet_manager.h
+++ b/src/wallet/api/wallet_manager.h
@@ -57,6 +57,13 @@ public:
                                              const std::string &viewKeyString,
                                              const std::string &spendKeyString = "",
                                              uint64_t kdf_rounds = 1) override;
+    Wallet * createDeterministicWalletFromSpendKey(std::string_view path,
+                                                            const std::string &password,
+                                                            const std::string &language,
+                                                            NetworkType nettype,
+                                                            uint64_t restoreHeight,
+                                                            const std::string &spendkey_string,
+                                                            uint64_t kdf_rounds = 1) override;
     Wallet * createWalletFromDevice(std::string_view path,
                                             const std::string &password,
                                             NetworkType nettype,
-- 
2.43.0

